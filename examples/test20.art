"=================================="
"Global & Function contexts"
"Testing scope"
"=================================="

another_func {
	print "in another_func: arg = " + @0
}

some_func {
	print "in some_func: arg = " + @0
	another_func: 2
	print "in some_func: arg = " + @0
}

some_func: 1

foreach #(1 2 3) {
	a @
	foreach #(1 2 3) {
		print "adding: " + a + " + " + @ + "=" + (a+@)
	}
}

fib { 
	if (@0<2) { 
		1 
	} { 
		$(fib: @0-1) + $(fib: @0-2) 
	} 
}

foreach $(range 1 20) { 
	print "fib(" + @ + ") = " + $(fib: @) 
}