h1: { print "<h1>" + @0 + "</h1>" }
h2: { print "<h2>" + @0 + "</h2>" }
h3: { print "<h3>" + @0 + "</h3>" }
h4: { print "<h4>" + @0 + "</h4>" }
h5: { print "<h5>" + @0 + "</h5>" }
h6: { print "<h6>" + @0 + "</h6>" }

p: { print "<p>" + @0 + "</p>" }
img: { print "<img src='" + @0 + "'/>" }
link: { print "<a href='" + @0 + "'>" + @1 + "</a>" }

html #{
	head: {

	}

	body {
		h1 "done"
		p "something"
		link "http://google.com" "Google"
	}
}

html.body

"This is 
a multiline string
with many different lines"

print $(markdown "# one two three")