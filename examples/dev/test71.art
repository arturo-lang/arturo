func {
	//if true { return $(!{ return 666 })} -> ~no
	//!{ return 1 }
	print "something"
	-> 2
}

arr #("one" "two" "three" "four")

len $(size arr)

print $(!func)
print len+2

isPrime [n]{ and n>1 $(all $(range 2 n-1) {n%@>0}) }
primes [upTo]{ filter $(range 1 upTo) isPrime }

print $(!primes 1000)

