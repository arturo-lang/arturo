name: "*nix"
on:
  push:
    branches-ignore:
      - 'drkameleon-devel'
    paths-ignore:
      - '*.gif'
      - '*.png'
      - '*.yml'
      - '*.md'
      - 'LICENSE'
  pull_request:
    paths-ignore:
      - '*.gif'
      - '*.png'
      - '*.yml'
      - '*.md'
      - 'LICENSE'

defaults:
  run:
    shell: bash -l {0}

jobs:
  cancel:
    name: "Cleanup"
    runs-on: ubuntu-latest
    steps:
      - name: "Cancel actions in progress of the same workflow"
        uses: styfle/cancel-workflow-action@0.6.0
        with:
          access_token: ${{ github.token }}

  # build-with-nimble:
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       os:
  #         - ubuntu-16.04
  #         - macos-latest
  #       flag:
  #         - ''
  #         - '-d:MINI'
  #   steps:
  #     - uses: actions/checkout@main
  #       with:
  #         submodules: recursive

  #     - name: Install Nim
  #       run: |
  #         wget -qO - https://nim-lang.org/choosenim/init.sh | bash -s -- -y
  #         echo "$HOME/.nimble/bin" >> $GITHUB_PATH

  #     - name: Install dependencies
  #       run: |
  #         sudo apt-fast update
  #         sudo apt-fast install -yq libgtk-3-dev libwebkit2gtk-4.0-dev
  #       if: matrix.os == 'ubuntu-16.04' && matrix.flag == ''

  #     - name: Verify dependecies
  #       run: |
  #         nim --version
          
  #     - name: Build Arturo
  #       run: |
  #         nimble build -Y ${{ matrix.flag }}

  # build-mini-with-shell:
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       os:
  #         - ubuntu-16.04
  #         - macos-latest
  #   steps:
  #     - uses: actions/checkout@main
  #       with:
  #         submodules: recursive

  #     - name: Install dependencies
  #       run: |
  #         wget -qO - https://nim-lang.org/choosenim/init.sh | bash -s -- -y
  #         echo "$HOME/.nimble/bin" >> $GITHUB_PATH
          
  #     - name: Verify dependecies
  #       run: |
  #         nim --version
          
  #     - name: Build Arturo
  #       run: |
  #         ./build.sh mini

  build:
    name: "Build & Test"
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-16.04
          - macos-latest
    steps:
      - uses: actions/checkout@main
        with:
          submodules: recursive

      - name: Install Nim
        run: |
          wget -qO - https://nim-lang.org/choosenim/init.sh | bash -s -- -y
          echo "$HOME/.nimble/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          sudo apt-fast update
          sudo apt-fast install -yq libgtk-3-dev libwebkit2gtk-4.0-dev
        if: matrix.os == 'ubuntu-16.04'

      # - name: Install MySQL
      #   run: |
      #     /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)" 
      #     brew install mysql
      #   if: matrix.os == 'macos-latest'
          
      - name: Verify dependecies
        run: |
          nim --version
          
      - name: Build Arturo
        run: |
          ./build.sh install

      - name: Run unit tests
        run: |
          echo "${{ github.workspace }}/bin" >> $GITHUB_PATH
          arturo tests/runAll.art
